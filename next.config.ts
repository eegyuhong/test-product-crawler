import type { NextConfig } from 'next';

const nextConfig: NextConfig = {
  /* config options here */
  images: {
    domains: ['image.oliveyoung.co.kr', 'img.29cm.co.kr', 'image.a-rt.com'],
  },
};

export default nextConfig;
